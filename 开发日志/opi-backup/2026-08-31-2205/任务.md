# 任务：Orange Pi 重装前备份（2026-08-31 22:05）

## 目标
用户要重装 Orange Pi 3B 系统，重装前把节点版本与配置完整备份到本地，
保证重装后能快速恢复受控。

## 完成内容
1. **收集 Pi 版本快照**（经云端 agent 通道，Pi SSH 不稳定）：
   - Debian 12 bookworm / aarch64 / RK356x
   - agent md5=180a985c813bac97d16702226cf45046 size=26477230（与本地 git 最新构建
     0a07cdcc / 17.9MB 不一致，是另一个构建）
   - Xray 26.3.27、ecp-xray-orangepi3b.service active、Tailscale 100.108.234.5
   - 1Panel 31252（openresty/mysql 容器 Exited 但 core 正常）
2. **备份 7 个关键配置文件到 docs/Backup-OPi-2026-0831/**：
   agent.yaml / ca.crt / client.crt / client.key / registration.key /
   xray-config.json（UUID 94d221ba-…）/ ecp-agent.service
   方式：云端 API → agent SHELL 通道 base64 输出 → 本地解码还原（SFTP 直连 Pi 卡死）
3. **编写恢复手册** docs/OPi-重装备份与恢复手册-20260831.md：
   - 已备份文件清单与原路径
   - 版本快照
   - 重装后 6 步恢复流程（依赖/配置/agent/xray/tailscale/验证）
   - 遗留与安全说明

## 关键决策
- **agent 二进制未强拉**：SSH/SFTP 大文件传输在 Pi 上卡死（Pi 系统负载问题，
  用户原本就要重装）。二进制可从云端 OTA 仓库或 git 源码重建，且重装后本用新版本。
- **relay.env 未备份**：root 所有且含 Cloudflare token，重装后可在控制面重新下发。
- **备份目录含私钥**：提醒勿推远程仓库（README/手册已注明）。

## 遗留问题
- [ ] Pi 上 180a985c 的 agent 二进制来源未知（与本地构建不一致），如需复核待 Pi 恢复正常
- [ ] agent.yaml 中 relay.token 为空（配置里未填），relay 通道 token 在 relay.env
- [ ] 重装后首次上线需确认节点能自动重新注册（registration.key 已备份可复用）

## 网络故障记录
- Pi SSH 22 端口通、认证通，但 exec/SFTP 长会话卡死（瞬间负载/IO 问题）
- 本机→云端 TLS 间歇性 EOF（多次重试后可用）
- 可靠路径：云端 API（agent 通道）★  >  Pi SSH 短命令 >  Pi SFTP/长输出（最差）