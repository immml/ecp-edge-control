# 本轮修复说明（2026-08-30 18:30）

针对你上一条消息的 4 个问题，全部处理完毕并已部署到云端 `38.76.180.246`。

## 1. 证书装了还是"不安全" —— 已修复 ✅

**根因不是 CA，是服务端证书缺公网 IP。**
- 你导入的 `ecp-cloud-ca.crt` 是对的（CA 指纹 `69:92:6C:A8…`，2046 年到期）
- 但服务端证书（你看到的 CN=ecp-control、17:15 签发、2027 到期那张）SAN 里只有 `localhost/127.0.0.1`，**没有 `38.76.180.246`**
- Chrome 只看 SAN 不看 CN → 即使信任 CA 也报不安全

**现在已修**：服务端证书 SAN 加入了公网 IP `38.76.180.246`，云端已重启。实测：
```
$ openssl s_client -connect 38.76.180.246:8443 -CAfile ecp-cloud-ca.crt
Verify return code: 0 (ok)
```
**你这边不用重装 CA**，重新打开 `https://38.76.180.246:8443` 刷新即可（若还缓存旧证书就强刷一次）。

## 2. 测速上传 0.00 —— 已修复 ✅

**根因**：speedtest-go 的 `--saving-mode`（省流量模式）会把上传数据量压到极小，Pi 上测出 0.00。

**修复**：改用完整模式 + 自动选延迟最低的服务器。Pi 真机复测：
```
节点 Hong Kong (Hong Kong)
延迟 52.5 ms (jitter 5.9)
下载 3.11 Mbps
上传 1.61 Mbps   ← 不再是 0.00
```
新 agent 已部署到 Pi，控制面板里直接重跑即可。

## 3. 公开脚本调研 + 集成 ✅

你点名的两个脚本都调研了：
| 脚本 | 结论 | 集成状态 |
|---|---|---|
| **xykt/IPQuality**（IP 质量体检） | 单文件 bash，`-j` 出 JSON，检测基础信息/IP类型/风险评分/流媒体解锁/邮局/DNS黑名单 | ✅ 已集成：节点上新增「IP 质量」检测（net_get ip_quality），首次自动下载脚本，输出 ASN/类型/风险分/流媒体解锁 |
| **spiritLHLS/ecs**（融合怪） | 全能测评（CPU/磁盘/三网测速/回程路由），但**纯文本输出 + 会改包管理器**，不适合 agent 调用 | ⚠️ 调研完成，未直接集成（作者自己也推荐新版 Go 版 oneclickvirt/ecs，后续需要可再议） |

## 4. 网络管理/组网改成按节点 —— 已重构 ✅

按你说的"应该放在节点的详细界面里面"：
- 节点详情页顶部新增**「网络」按钮** → 进入 `/nodes/:id/network`（该节点的组网 + 网络管理 + VPN 跳板）
- 原"组网管理"全局页改为**节点选择器 + 三 Tab**：组网（Tailscale/FRP）/ 网络管理（WiFi/网卡/测速/虚拟MAC）/ VPN 跳板（Clash）
- 新增 `NodeMeshPanel` 单节点组件：Tailscale 登录/停用、FRP 实例启停/编辑 ini，全部按节点操作

## 5. VPN 模型改成"每节点一个跳板" —— 已重构 ✅

**新模型**：每个边缘盒子 = 一个独立 VPN 出口节点（拿盒子做跳板），不再是单一网关。
- Clash 导出支持**多节点数组**：每个盒子生成一个 vmess proxy，可选（SELECT）或自动（url-test）
- `deploy/xray-vpn.sh` 加 `--name` 参数：每个节点独立 systemd 单元 + 配置 + 自己的 Cloudflare Tunnel 域名
- 前端"VPN 跳板"页可手动添加或**从在线节点一键填充**多行跳板

**盒子侧部署（每台盒子执行一次，root）**：
```bash
sudo bash deploy/xray-vpn.sh --name node1
```
然后到 Cloudflare Zero Trust 给这台盒子建一条 Tunnel：`node1.vpn.你域名 → http://127.0.0.1:8444`，把域名+UUID 填到控制面板导出 Clash。

---
**已部署**：云端 server（证书 SAN + 多跳板 API + 新前端）✅ / Pi agent（测速修复 + IP 质量）✅
**Git**：`ecd364f` 已推送 `github.com/immml/ecp-edge-control`